apiVersion: v1
kind: Service
metadata:
  name: medical-qa-retrieval
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: medical-qa-retrieval
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
