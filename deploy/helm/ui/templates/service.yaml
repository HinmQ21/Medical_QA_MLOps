apiVersion: v1
kind: Service
metadata:
  name: medical-qa-ui
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: medical-qa-ui
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
