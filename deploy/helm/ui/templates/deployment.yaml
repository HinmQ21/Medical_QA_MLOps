apiVersion: apps/v1
kind: Deployment
metadata:
  name: medical-qa-ui
  labels:
    app.kubernetes.io/name: medical-qa-ui
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: medical-qa-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: medical-qa-ui
    spec:
      containers:
        - name: ui
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: medical-qa-ui-config
          env:
            # Gateway API key, reused from the nginx Secret; attached server-side
            # (never reaches the browser). Not optional: the Secret must exist before
            # install — scripts/cloud/create_secrets.sh creates it / deploy nginx first.
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.auth.existingSecret | quote }}
                  key: API_KEY
          ports:
            - name: http
              containerPort: 8501
          livenessProbe:
            httpGet:
              path: /_stcore/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /_stcore/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            # Streamlit cold start (import + first render) can exceed the default
            # 3 failures; ~60s grace keeps the pod from flapping NotReady on boot.
            failureThreshold: 6
          resources:
{{ toYaml .Values.resources | indent 12 }}
