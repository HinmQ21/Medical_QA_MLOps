apiVersion: v1
kind: Service
metadata:
  name: medical-qa-api
  labels:
    app.kubernetes.io/name: medical-qa-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: medical-qa-api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
